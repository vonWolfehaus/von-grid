@require '../variables';

form-map-settings {
	flex-direction: column;
	width: 260px;
	margin-top: 15px;

	font-size: 0.8em;
	color: #fff;

	& > * {
		width: 100%;
	}

	button {
		float: right;
		width: auto;
	}

	span > button {
		margin-right: 0;
	}
}